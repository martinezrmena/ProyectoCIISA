<?xml version="1.0" encoding="utf-8" ?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CIISA.RetailOnLine.Framework.External.ResizableColumnsListView.TemplateExpandableGridTF">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0"/>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="270"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="85"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Style="{StaticResource ListviewLabel}"  Text="{Binding Estilo}" TextColor="{Binding ItemTextColor}"/>
        <Label Grid.Column="1" Style="{StaticResource ListviewLabel}"  Text="{Binding Codigo}" TextColor="{Binding ItemTextColor}"/>
        <Label Grid.Column="2" Style="{StaticResource ListviewLabel}"  Text="{Binding Descripcion}" TextColor="{Binding ItemTextColor}"/>
        <Label Grid.Column="3" Style="{StaticResource ListviewLabel}"  Text="{Binding Cantidad}" TextColor="{Binding ItemTextColor}"/>
        <Label Grid.Column="4" Style="{StaticResource ListviewLabel}"  Text="{Binding TomaFisica}" TextColor="{Binding ItemTextColor}"/>
        <Label Grid.Column="5" Style="{StaticResource ListviewLabel}"  Text="{Binding Diferencia}" TextColor="{Binding ItemTextColor}"/>
    </Grid>

</ViewCell>